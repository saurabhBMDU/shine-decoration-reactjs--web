

const intialState = {
    data:{},
    loading: false,
    error:null
}

const singleOrder = (state=intialState,action)=>{
    switch(action.type){
        default: return state
    }
}

export default singleOrder;